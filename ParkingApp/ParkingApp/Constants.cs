namespace ParkingApp
{
    public class Constants
    {
        public enum RegistrationFragments
        {
            Welcome,
            MobileNumber,
            VerifyPin,
            Name,
            Email
        }

        public enum RentFragments
        {
            RentDetails,
            PickUp,
            CurrentRent,
            ReturnCheckList
        }

        public enum CameraFragments
        {
            TakePhoto,
            PhotoPreview
        }
    }
}